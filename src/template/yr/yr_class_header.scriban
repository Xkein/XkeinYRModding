{{  include 'yr/yrpp.scriban'
    if !need_codegen(cpp_class)
        ret
    end
    include 'include/common.scriban'
    init_extra_vars(this)
-}}
#pragma once
#include "{{cpp_class.source_file}}"

class {{gen_type_name}}
{
public:
    static void Register();
    static void Unregister();
};